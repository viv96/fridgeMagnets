const postSchema = {
  // use this for dynamoDB
  title: String,
  message: String,
  creator: String,
  tags: [String],
  selectedFile: String,
  likeCount: {
    type: Number,
    default: 0,
  },
  createdAt: {
    type: Date,
    default: new Date(),
  },
};

const PostMessage = postSchema;

module.exports = PostMessage;
